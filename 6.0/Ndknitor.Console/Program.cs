using System.Diagnostics;
using Ndknitor.System;
using Newtonsoft.Json;

// int[] arrayToSort = { 4, 2, 7, 1, 9, 5, 3, 8, 6 };

// arrayToSort = arrayToSort.ParallelMergeSort();

// foreach (var item in arrayToSort)
// {
//     Console.WriteLine(item);
// }
